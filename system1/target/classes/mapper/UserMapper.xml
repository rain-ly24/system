<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">
    <select id="findById" parameterType="int"
            resultType="com.ly.pojo.User">
        select * from s_student where id = #{id}
    </select>
    <!--根据sid查询课程信息 -->
    <select id="findBySid" parameterType="int"
            resultType="com.ly.pojo.User">
        select * from s_student where sid = #{sid}
    </select>

    <select id="findAll"
            resultType="com.ly.pojo.User">
        select * from s_student
    </select>
    <!--修改课程信息-->
    <update id="updateCourse" parameterType="com.ly.pojo.User">
        update s_student set name= #{name},hours = #{hours},sid= #{sid} where
            id = #{id}
    </update>
    <!-- 插入课程信息 -->
    <insert id="addCourse" parameterType="com.ly.pojo.User">
        insert into s_student(id,name,hours,sid)values
            (#{id},#{name},#{hours},#{sid})
    </insert>

    <!-- 删除课程信息 -->
    <delete id="deleteCourse" parameterType="int">
        delete from s_student where id=#{id}
    </delete>
</mapper>
